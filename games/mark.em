# How one letter of a guess turned out. A module file's declarations are the project's,
# so the enum is reached as Mark from anywhere — the file name only groups it.

enum Mark { HIT, PRESENT, MISS }
