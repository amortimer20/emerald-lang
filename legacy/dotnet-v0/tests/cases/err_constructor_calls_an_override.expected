
err_constructor_calls_an_override.em:17  A constructor cannot call describe on the object it is building.

   17 |         print(self.describe())

  The object is not finished yet, and describe may be overridden by a subclass whose own fields are still unset.
  Call it after the object exists:  var it = Base(...)
  it.describe()

  emerald explain
