
err_visibility_other_class.em:7  _balance belongs to Account — that is what the _ says.

    7 |     func peek(account: Account): Int { return account._balance }

  A name starting with _ can be used inside Account, and inside any class that extends it. Drop the _ to let the rest of the program use it.
