
warn_predicate_names.em:2  warning: ready returns Bool, so its name ends in ? — ready?

    2 | func ready(n: Int): Bool { return n > 0 }

  A name ending in ? means the answer is yes or no, and the reader learns the return type without looking the method up.

warn_predicate_names.em:3  warning: size? ends in ?, so it is expected to return Bool, not Int.

    3 | func size?(n: Int): Int { return n }

  The ? is a promise about the answer. Drop it, or return Bool.
true
2
