# An enum is a type, so a file holding one contributes it to the project the same way a
# file holding a class does. Folded into a module it would be a member with no syntax to
# reach it, and the file could declare something nothing else could name.
enum Suit { HEARTS, SPADES }
