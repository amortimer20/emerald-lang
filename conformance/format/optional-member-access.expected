# Section 4.2's `?.` survives formatting: dropping the `?` would change what
# the program means.
const name: String? = "ada"
print(name?.count, name?.upper())
const shout = { => name?.upper()?.count }
