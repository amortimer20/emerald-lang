   # a line comment, indented past where it will land
var count = 0 # a trailing comment on the same line
## documentation for the function below
func step() {
    #[
      a block comment
        keeps its own internal layout
    ]#
    count += 1 # incremented here
}
