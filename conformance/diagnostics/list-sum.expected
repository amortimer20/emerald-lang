diagnostics/list-sum.em:1:22: `sum` needs a List of Ints or Floats, but this is List[String]
  print(["one", "two"].sum())
                       ^^^
Use `map` to produce numbers first, or choose an aggregation that fits this List's elements.
diagnostics/list-sum.em:2:11: `sum` takes 0 arguments, but this call passes 1
  print([1].sum(1))
            ^^^
Match the number of arguments to what the method needs.
diagnostics/list-sum.em:3:15: `sum` takes 0 arguments, but this call passes 1
  print(["one"].sum(1))
                ^^^
Match the number of arguments to what the method needs.
