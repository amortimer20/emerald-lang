# Section 5.1: the closing quotes' indentation is removed from every line, so
# a line indented less than them has nothing to remove it from.

var message = """
    Dear reader,
  this line is indented less than the closing quotes.
    """
