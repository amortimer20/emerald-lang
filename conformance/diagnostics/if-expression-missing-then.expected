diagnostics/if-expression-missing-then.em:1:23: an `if` used as a value needs `then` after its condition
  const value = if true 1 else 2
                        ^
Write `if condition then value else other_value`. Use braces when the `if` runs statements instead.
