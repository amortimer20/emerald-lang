diagnostics/list-filtering.em:1:33: this lambda produces Int, but Bool is expected here
  print([1, 2].filter { number => number })
                                  ^^^^^^
Produce a value of the expected type, or convert it first.
diagnostics/list-filtering.em:2:14: `reject` takes 1 block, but this call passes 0 arguments
  print([1, 2].reject())
               ^^^^^^
Write the block after the method, as in `numbers.each { number => print(number) }`.
