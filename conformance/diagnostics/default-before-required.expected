diagnostics/default-before-required.em:1:39: `name` has no default, so it cannot follow `punctuation`, which has one
  func greet(punctuation: String = "!", name: String) {
                                        ^^^^
Move the parameters with defaults to the end, or give this one a default too.
