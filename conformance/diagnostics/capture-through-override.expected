diagnostics/capture-through-override.em:23:7: `greet` reads `title`, which is not assigned yet here
  print(greeter.greet())
        ^^^^^^^^^^^^^^^
Move this call below the line that assigns `title`.
diagnostics/capture-through-override.em:45:7: `title` reads `suffix`, which is not assigned yet here
  print(titled.title())
        ^^^^^^^^^^^^^^
Move this call below the line that assigns `suffix`.
diagnostics/capture-through-override.em:80:7: `ordered` reads `offset`, which is not assigned yet here
  print(ordered())
        ^^^^^^^^^
Move this call below the line that assigns `offset`.
diagnostics/capture-through-override.em:82:7: `add` reads `offset`, which is not assigned yet here
  print(Step(1) + Step(2))
        ^^^^^^^^^^^^^^^^^
Move this call below the line that assigns `offset`.
