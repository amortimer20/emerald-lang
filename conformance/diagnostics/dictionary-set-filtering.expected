diagnostics/dictionary-set-filtering.em:2:36: this lambda produces Int, but Bool is expected here
  print(ages.filter { (name, age) => age })
                                     ^^^
Produce a value of the expected type, or convert it first.
diagnostics/dictionary-set-filtering.em:5:14: `reject` takes 1 block, but this call passes 0 arguments
  print(values.reject())
               ^^^^^^
Write the block after the method, as in `numbers.reject { number => print(number) }`.
