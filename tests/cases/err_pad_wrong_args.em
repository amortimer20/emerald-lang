# The fill is optional, so the built-in signatures had to learn to say "one or two" -
# they could only ever say an exact count, while an ordinary function had defaults.
print("ab".pad_right(6, ".", "extra"))
