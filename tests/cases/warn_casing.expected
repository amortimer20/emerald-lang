
warn_casing.em:3  warning: A variable is written in snake_case, so playerName reads as a type.

    3 | var playerName = "ana"

  Rename it to player_name. Capitalised names mean types in Emerald.

warn_casing.em:4  warning: Constants are written in SCREAMING_SNAKE_CASE, so maxScore reads as a variable.

    4 | const maxScore = 100

  Rename it to MAX_SCORE.

warn_casing.em:6  warning: Type names are written in PascalCase, so player_stats reads as something else.

    6 | class player_stats {

  Rename it to PlayerStats. Types are capitalised; nothing else is.

warn_casing.em:7  warning: An instance variable is written in snake_case, so Health reads as a type.

    7 |     var Health: Int

  Rename it to health. Capitalised names mean types in Emerald.

warn_casing.em:8  warning: A parameter is written in snake_case, so Health reads as a type.

    8 |     constructor(Health: Int) { self.Health = Health }

  Rename it to health. Capitalised names mean types in Emerald.

warn_casing.em:11  warning: A function is written in snake_case, so GetScore reads as a type.

   11 | func GetScore(playerId: Int): Int { return playerId * 2 }

  Rename it to get_score. Capitalised names mean types in Emerald.

warn_casing.em:11  warning: A parameter is written in snake_case, so playerId reads as a type.

   11 | func GetScore(playerId: Int): Int { return playerId * 2 }

  Rename it to player_id. Capitalised names mean types in Emerald.
ana
100
6
5
