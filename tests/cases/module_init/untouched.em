## Nothing reaches this file, so nothing in it runs. That is the whole reason §3.3 chose
## first access over startup: a file does not act merely by existing, which is where
## Python's import-order problems come from.
print("this line must never appear")

var unused = 1
