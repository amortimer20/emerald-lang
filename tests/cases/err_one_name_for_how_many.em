## One name for "how many", whatever is being asked. String answered only to .length
## while every container answered only to .count — two names split by receiver type,
## which is the thing Java is known for and the reason this is one name now.
var s = "hi"
print(s.length())
