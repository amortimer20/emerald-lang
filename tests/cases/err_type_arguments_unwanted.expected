
err_type_arguments_unwanted.em:4  count does not take a type in angle brackets.

    4 | print([1].count<Int>())

  Drop them:  count(...)   —   as is the one that takes a type, for a value whose type is not known yet:  value.as<Dog>()
