
err_narrowing_stale.em:12  This is String?, not String, so length may not exist.

   12 |     print(name.length)

  Checking name does not settle it here: a function assigns name, so it could have changed in between. Copy it first and check the copy, which nothing else can reach:
  var it = name
  if it != nothing { ... }

  emerald explain
