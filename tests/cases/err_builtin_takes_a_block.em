## The one built-in that cannot be named without calling it, and for a reason rather
## than by being on a list: what map answers is whatever the block answers, so there is
## no shape to write down until a block is written.
var xs = [3, 1, 2]
var f = xs.map
