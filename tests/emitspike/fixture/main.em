## The smallest program the emitter can take, chosen to be a real (if trivial) two-line
## program rather than a single print. Proof that statements run in order, not proof
## that the emitter can do more than it can.
print("Hello from CIL")
print("Emerald compiles.")
